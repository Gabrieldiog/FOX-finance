services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: fox
      POSTGRES_PASSWORD: fox
      POSTGRES_DB: fox_dev
    ports:
      - "5432:5432"
    volumes:
      - fox_pgdata:/var/lib/postgresql/data

volumes:
  fox_pgdata:
